# dynamic-compile-proxy
Create proxies at compile time to handle objects

```

Spring 					    | JSR-330
-----------------------------------------
@Autowired				    | @Inject
@Component 				    | @Named
@Scope					    | @Scope
@Scope("singleton")         | @Singleton
@Qualifier				    | @Named

```